# OceaneGetServiceController — Zones à valider
Généré le : 2026-04-26 09:46 | 113 flags | 0 insights LLM

## ⚠️ Gaps de logique — Comportements non définis
- [ ] **[block_1]** ⬜ non enrichi
  - Fragment : `$this->app->get('AstroBase')->isJson($detailTicketJson)`
  - Question : Point de décision — 'this'->app->get('AstroBase')->isJson('detailTicketJson'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_2]** ⬜ non enrichi
  - Fragment : `key_exists('relatedResource', $this->oceaneData) && is_array($this->oceaneData['relatedResource']) &`
  - Question : Point de décision — key_exists('relatedResource', 'this'->oceaneData) et is_array('this'->oceaneData['relatedResource']) et key_exists('resourceSpecCharacteristic', 'this'->oceaneData['relatedResource']) et is_array('this'->oceaneData['relatedResource']['resourceSpecCharacteristic']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_3]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('index', $value) && !is_null($id) && (intval($value['index']) == $id)`
  - Question : Point de décision — is_array('value') et key_exists('index', 'value') et !is_null('id') et (intval('value'['index']) égal à 'id') et key_exists('value', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_4]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('index', $value) && key_exists('value', $value)`
  - Question : Point de décision — is_array('value') et key_exists('index', 'value') et key_exists('value', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_5]** ⬜ non enrichi
  - Fragment : `key_exists('relatedService', $this->oceaneData) && is_array($this->oceaneData['relatedService']) && `
  - Question : Point de décision — key_exists('relatedService', 'this'->oceaneData) et is_array('this'->oceaneData['relatedService']) et key_exists('serviceSpecCharacteristic', 'this'->oceaneData['relatedService']) et is_array('this'->oceaneData['relatedService']['serviceSpecCharacteristic']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_6]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('index', $value) && !is_null($id) && (intval($value['index']) == $id)`
  - Question : Point de décision — is_array('value') et key_exists('index', 'value') et !is_null('id') et (intval('value'['index']) égal à 'id') et key_exists('value', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_7]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('index', $value) && key_exists('value', $value)`
  - Question : Point de décision — is_array('value') et key_exists('index', 'value') et key_exists('value', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_8]** ⬜ non enrichi
  - Fragment : `key_exists('relatedResource', $this->oceaneData) && is_array($this->oceaneData['relatedResource']) &`
  - Question : Point de décision — key_exists('relatedResource', 'this'->oceaneData) et is_array('this'->oceaneData['relatedResource']) et key_exists('resourceSpecification', 'this'->oceaneData['relatedResource']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_9]** ⬜ non enrichi
  - Fragment : `$idAndName`
  - Question : Point de décision — 'idAndName'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_10]** ⬜ non enrichi
  - Fragment : `key_exists('relatedService', $this->oceaneData) && is_array($this->oceaneData['relatedService']) && `
  - Question : Point de décision — key_exists('relatedService', 'this'->oceaneData) et is_array('this'->oceaneData['relatedService']) et key_exists('serviceSpecification', 'this'->oceaneData['relatedService']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_11]** ⬜ non enrichi
  - Fragment : `$idAndName`
  - Question : Point de décision — 'idAndName'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_12]** ⬜ non enrichi
  - Fragment : `key_exists('troubleTicketCharacteristic', $this->oceaneData) && is_array($this->oceaneData['troubleT`
  - Question : Point de décision — key_exists('troubleTicketCharacteristic', 'this'->oceaneData) et is_array('this'->oceaneData['troubleTicketCharacteristic']) et count('this'->oceaneData['troubleTicketCharacteristic']) > 0. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_13]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('index', $value) && !is_null($champ) && (intval($value['index']) == $`
  - Question : Point de décision — is_array('value') et key_exists('index', 'value') et !is_null('champ') et (intval('value'['index']) égal à 'champ') et key_exists('value', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_14]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('index', $value) && key_exists('value', $value)`
  - Question : Point de décision — is_array('value') et key_exists('index', 'value') et key_exists('value', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_15]** ⬜ non enrichi
  - Fragment : `key_exists('priority', $this->oceaneData) && is_array($this->oceaneData['priority'])`
  - Question : Point de décision — key_exists('priority', 'this'->oceaneData) et is_array('this'->oceaneData['priority']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_16]** ⬜ non enrichi
  - Fragment : `$idAndLabel`
  - Question : Point de décision — 'idAndLabel'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_17]** ⬜ non enrichi
  - Fragment : `key_exists('urgency', $this->oceaneData) && is_array($this->oceaneData['urgency'])`
  - Question : Point de décision — key_exists('urgency', 'this'->oceaneData) et is_array('this'->oceaneData['urgency']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_18]** ⬜ non enrichi
  - Fragment : `$idAndLabel`
  - Question : Point de décision — 'idAndLabel'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_19]** ⬜ non enrichi
  - Fragment : `key_exists('creationDate', $this->oceaneData)`
  - Question : Point de décision — key_exists('creationDate', 'this'->oceaneData). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_20]** ⬜ non enrichi
  - Fragment : `!is_null($format)`
  - Question : Point de décision — !is_null('format'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_21]** ⬜ non enrichi
  - Fragment : `key_exists('detectionDate', $this->oceaneData)`
  - Question : Point de décision — key_exists('detectionDate', 'this'->oceaneData). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_22]** ⬜ non enrichi
  - Fragment : `!is_null($format)`
  - Question : Point de décision — !is_null('format'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_23]** ⬜ non enrichi
  - Fragment : `key_exists('troubleCause', $this->oceaneData) && is_array($this->oceaneData['troubleCause']) && coun`
  - Question : Point de décision — key_exists('troubleCause', 'this'->oceaneData) et is_array('this'->oceaneData['troubleCause']) et count('this'->oceaneData['troubleCause']) > 0. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_24]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('problemDetail', $value) && key_exists('id', $value['problemDetail'])`
  - Question : Point de décision — is_array('value') et key_exists('problemDetail', 'value') et key_exists('id', 'value'['problemDetail']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_25]** ⬜ non enrichi
  - Fragment : `key_exists('relatedParty', $this->oceaneData) && is_array($this->oceaneData['relatedParty']) && coun`
  - Question : Point de décision — key_exists('relatedParty', 'this'->oceaneData) et is_array('this'->oceaneData['relatedParty']) et count('this'->oceaneData['relatedParty']) > 0. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_26]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('role', $value) && $value['role'] == 'TroubleResolutionLeader' && key`
  - Question : Point de décision — is_array('value') et key_exists('role', 'value') et 'value'['role'] égal à 'TroubleResolutionLeader' et key_exists('id', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_27]** ⬜ non enrichi
  - Fragment : `key_exists('relatedParty', $this->oceaneData) && is_array($this->oceaneData['relatedParty']) && coun`
  - Question : Point de décision — key_exists('relatedParty', 'this'->oceaneData) et is_array('this'->oceaneData['relatedParty']) et count('this'->oceaneData['relatedParty']) > 0. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_28]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('role', $value) && $value['role'] == 'troubleTicketOriginator' && key`
  - Question : Point de décision — is_array('value') et key_exists('role', 'value') et 'value'['role'] égal à 'troubleTicketOriginator' et key_exists('id', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_29]** ⬜ non enrichi
  - Fragment : `key_exists('partyIntervention', $this->oceaneData) && is_array($this->oceaneData['partyIntervention'`
  - Question : Point de décision — key_exists('partyIntervention', 'this'->oceaneData) et is_array('this'->oceaneData['partyIntervention']) et count('this'->oceaneData['partyIntervention']) > 0. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_30]** ⬜ non enrichi
  - Fragment : `is_array($value['relatedParty'])`
  - Question : Point de décision — is_array('value'['relatedParty']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_31]** ⬜ non enrichi
  - Fragment : `is_array($valueRelatedParty) && key_exists('actionInProgress', $valueRelatedParty) && $valueRelatedP`
  - Question : Point de décision — is_array('valueRelatedParty') et key_exists('actionInProgress', 'valueRelatedParty') et 'valueRelatedParty'['role'] égal à 'TroubleResolutionLeader'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_32]** ⬜ non enrichi
  - Fragment : `is_array($valueRelatedParty['actionInProgress']) && key_exists('description', $valueRelatedParty['ac`
  - Question : Point de décision — is_array('valueRelatedParty'['actionInProgress']) et key_exists('description', 'valueRelatedParty'['actionInProgress'])) return 'valueRelatedParty'['actionInProgress']['description']; } } } } } return null; } public function getActifGroup(. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_33]** ⬜ non enrichi
  - Fragment : `key_exists('relatedParty', $this->oceaneData) && is_array($this->oceaneData['relatedParty']) && coun`
  - Question : Point de décision — key_exists('relatedParty', 'this'->oceaneData) et is_array('this'->oceaneData['relatedParty']) et count('this'->oceaneData['relatedParty']) > 0. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_34]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('role', $value) && $value['role'] == 'TroubleResolutionContributor' &`
  - Question : Point de décision — is_array('value') et key_exists('role', 'value') et 'value'['role'] égal à 'TroubleResolutionContributor' et key_exists('id', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_35]** ⬜ non enrichi
  - Fragment : `key_exists('partyIntervention', $this->oceaneData) && is_array($this->oceaneData['partyIntervention'`
  - Question : Point de décision — key_exists('partyIntervention', 'this'->oceaneData) et is_array('this'->oceaneData['partyIntervention']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_36]** ⬜ non enrichi
  - Fragment : `key_exists('interventionStatus', $value) && is_array($value['interventionStatus'])`
  - Question : Point de décision — key_exists('interventionStatus', 'value') et is_array('value'['interventionStatus']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_37]** ⬜ non enrichi
  - Fragment : `(is_array($status) && key_exists('status', $status))`
  - Question : Point de décision — (is_array('status') et key_exists('status', 'status')). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_38]** ⬜ non enrichi
  - Fragment : `key_exists('status', $this->oceaneData) && is_array($this->oceaneData['status'])`
  - Question : Point de décision — key_exists('status', 'this'->oceaneData) et is_array('this'->oceaneData['status']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_39]** ⬜ non enrichi
  - Fragment : `key_exists('isCurrentStatus', $value) && $value['isCurrentStatus'] == 1`
  - Question : Point de décision — key_exists('isCurrentStatus', 'value') et 'value'['isCurrentStatus'] égal à 1. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_40]** ⬜ non enrichi
  - Fragment : `key_exists('ticketType', $this->oceaneData) && is_array($this->oceaneData['ticketType'])`
  - Question : Point de décision — key_exists('ticketType', 'this'->oceaneData) et is_array('this'->oceaneData['ticketType']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_41]** ⬜ non enrichi
  - Fragment : `$idAndLabel`
  - Question : Point de décision — 'idAndLabel'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_42]** ⬜ non enrichi
  - Fragment : `key_exists('origin', $this->oceaneData) && is_array($this->oceaneData['origin'])`
  - Question : Point de décision — key_exists('origin', 'this'->oceaneData) et is_array('this'->oceaneData['origin']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_43]** ⬜ non enrichi
  - Fragment : `$idAndLabel`
  - Question : Point de décision — 'idAndLabel'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_44]** ⬜ non enrichi
  - Fragment : `key_exists('relatedResource', $this->oceaneData) && is_array($this->oceaneData['relatedResource']) &`
  - Question : Point de décision — key_exists('relatedResource', 'this'->oceaneData) et is_array('this'->oceaneData['relatedResource']) et key_exists('id', 'this'->oceaneData['relatedResource']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_45]** ⬜ non enrichi
  - Fragment : `key_exists('relatedResource', $this->oceaneData) && is_array($this->oceaneData['relatedResource']) &`
  - Question : Point de décision — key_exists('relatedResource', 'this'->oceaneData) et is_array('this'->oceaneData['relatedResource']) et key_exists('resourceCharacteristic', 'this'->oceaneData['relatedResource']) et is_array('this'->oceaneData['relatedResource']['resourceCharacteristic']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_46]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('@type', $value) && $value['@type'] == $param && key_exists('id', $va`
  - Question : Point de décision — is_array('value') et key_exists('@type', 'value') et 'value'['@type'] égal à 'param' et key_exists('id', 'value') et 'value'['id'] égal à 'name'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_47]** ⬜ non enrichi
  - Fragment : `key_exists('relatedParty', $this->oceaneData) && is_array($this->oceaneData['relatedParty']) && coun`
  - Question : Point de décision — key_exists('relatedParty', 'this'->oceaneData) et is_array('this'->oceaneData['relatedParty']) et count('this'->oceaneData['relatedParty']) > 0. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_48]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('familyName', $value) && key_exists('id', $value)`
  - Question : Point de décision — is_array('value') et key_exists('familyName', 'value') et key_exists('id', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_49]** ⬜ non enrichi
  - Fragment : `key_exists('troubleCause', $this->oceaneData) && is_array($this->oceaneData['troubleCause'])`
  - Question : Point de décision — key_exists('troubleCause', 'this'->oceaneData) et is_array('this'->oceaneData['troubleCause']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_50]** ⬜ non enrichi
  - Fragment : `key_exists('partyIntervention', $this->oceaneData) && is_array($this->oceaneData['partyIntervention'`
  - Question : Point de décision — key_exists('partyIntervention', 'this'->oceaneData) et is_array('this'->oceaneData['partyIntervention']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_51]** ⬜ non enrichi
  - Fragment : `key_exists('relatedParty', $this->oceaneData) && is_array($this->oceaneData['relatedParty']) && coun`
  - Question : Point de décision — key_exists('relatedParty', 'this'->oceaneData) et is_array('this'->oceaneData['relatedParty']) et count('this'->oceaneData['relatedParty']) > 0. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_52]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('role', $value) && $value['role'] == 'WorkingGroup' && key_exists('id`
  - Question : Point de décision — is_array('value') et key_exists('role', 'value') et 'value'['role'] égal à 'WorkingGroup' et key_exists('id', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_53]** ⬜ non enrichi
  - Fragment : `key_exists('status', $this->oceaneData) && is_array($this->oceaneData['status'])`
  - Question : Point de décision — key_exists('status', 'this'->oceaneData) et is_array('this'->oceaneData['status']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_54]** ⬜ non enrichi
  - Fragment : `key_exists('reason', $value)`
  - Question : Point de décision — key_exists('reason', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_55]** ⬜ non enrichi
  - Fragment : `key_exists('relatedResource', $this->oceaneData) && is_array($this->oceaneData['relatedResource']) &`
  - Question : Point de décision — key_exists('relatedResource', 'this'->oceaneData) et is_array('this'->oceaneData['relatedResource']) et key_exists('resourceSpecification', 'this'->oceaneData['relatedResource']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_56]** ⬜ non enrichi
  - Fragment : `$this->app->get('AstroBase')->isJson($resultChild)`
  - Question : Point de décision — 'this'->app->get('AstroBase')->isJson('resultChild'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_57]** ⬜ non enrichi
  - Fragment : `is_array($resultChild) && !empty($resultChild) && !key_exists('code', $resultChild) && key_exists('t`
  - Question : Point de décision — is_array($resultChild) && !le champ 'resultChild' est vide && !key_exists('code', $resultChild) && key_exists('type', $resultChild[0]) && $resultChild[0]['type'] == 'isParent'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_58]** ⬜ non enrichi
  - Fragment : `key_exists('partyIntervention', $this->oceaneData) && is_array($this->oceaneData['partyIntervention'`
  - Question : Point de décision — key_exists('partyIntervention', 'this'->oceaneData) et is_array('this'->oceaneData['partyIntervention']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_59]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('relatedParty', $value) && is_array($value['relatedParty'])`
  - Question : Point de décision — is_array('value') et key_exists('relatedParty', 'value') et is_array('value'['relatedParty']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_60]** ⬜ non enrichi
  - Fragment : `is_array($party) && key_exists('@referredType', $party) && $party['@referredType'] == 'Organisation'`
  - Question : Point de décision — is_array('party') et key_exists('@referredType', 'party') et 'party'['@referredType'] égal à 'Organisation' et key_exists('role', 'party') et 'party'['role'] égal à 'TroubleResolutionContributor' et key_exists('id', 'party') et 'party'['id'] égal à 'eds'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_61]** ⬜ non enrichi
  - Fragment : `$isContributor && key_exists('interventionStatus', $value) && is_array($value['interventionStatus'])`
  - Question : Point de décision — 'isContributor' et key_exists('interventionStatus', 'value') et is_array('value'['interventionStatus']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_62]** ⬜ non enrichi
  - Fragment : `$activationRequested`
  - Question : Point de décision — 'activationRequested'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_63]** ⬜ non enrichi
  - Fragment : `key_exists('status', $this->oceaneData) && is_array($this->oceaneData['status'])`
  - Question : Point de décision — key_exists('status', 'this'->oceaneData) et is_array('this'->oceaneData['status']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_64]** ⬜ non enrichi
  - Fragment : `key_exists('code', $value) && $value['code'] == "Restored"`
  - Question : Point de décision — key_exists('code', 'value') et 'value'['code'] égal à "Restored". Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_65]** ⬜ non enrichi
  - Fragment : `key_exists('startDate', $value)`
  - Question : Point de décision — key_exists('startDate', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_66]** ⬜ non enrichi
  - Fragment : `!is_null($format)`
  - Question : Point de décision — !is_null('format'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_67]** ⬜ non enrichi
  - Fragment : `key_exists('status', $this->oceaneData) && is_array($this->oceaneData['status'])`
  - Question : Point de décision — key_exists('status', 'this'->oceaneData) et is_array('this'->oceaneData['status']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_68]** ⬜ non enrichi
  - Fragment : `key_exists('code', $value) && $value['code'] == "Resolved"`
  - Question : Point de décision — key_exists('code', 'value') et 'value'['code'] égal à "Resolved". Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_69]** ⬜ non enrichi
  - Fragment : `key_exists('startDate', $value)`
  - Question : Point de décision — key_exists('startDate', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_70]** ⬜ non enrichi
  - Fragment : `!is_null($format)`
  - Question : Point de décision — !is_null('format'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_71]** ⬜ non enrichi
  - Fragment : `key_exists('category', $this->oceaneData) && is_array($this->oceaneData['category'])`
  - Question : Point de décision — key_exists('category', 'this'->oceaneData) et is_array('this'->oceaneData['category']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_72]** ⬜ non enrichi
  - Fragment : `$idAndLabel`
  - Question : Point de décision — 'idAndLabel'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_73]** ⬜ non enrichi
  - Fragment : `key_exists('troubleCause', $this->oceaneData) && is_array($this->oceaneData['troubleCause'])`
  - Question : Point de décision — key_exists('troubleCause', 'this'->oceaneData) et is_array('this'->oceaneData['troubleCause']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_74]** ⬜ non enrichi
  - Fragment : `key_exists('partyIntervention', $this->oceaneData) && is_array($this->oceaneData['partyIntervention'`
  - Question : Point de décision — key_exists('partyIntervention', 'this'->oceaneData) et is_array('this'->oceaneData['partyIntervention']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_75]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('relatedParty', $value) && is_array($value['relatedParty'])`
  - Question : Point de décision — is_array('value') et key_exists('relatedParty', 'value') et is_array('value'['relatedParty']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_76]** ⬜ non enrichi
  - Fragment : `is_array($party) && key_exists('@referredType', $party) && $party['@referredType'] == 'Organisation'`
  - Question : Point de décision — is_array('party') et key_exists('@referredType', 'party') et 'party'['@referredType'] égal à 'Organisation' et key_exists('role', 'party') et 'party'['role'] égal à 'TroubleResolutionContributor' et key_exists('id', 'party'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_77]** ⬜ non enrichi
  - Fragment : `$isContributor && key_exists('interventionStatus', $value) && is_array($value['interventionStatus'])`
  - Question : Point de décision — 'isContributor' et key_exists('interventionStatus', 'value') et is_array('value'['interventionStatus']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_78]** ⬜ non enrichi
  - Fragment : `$activationAccepted`
  - Question : Point de décision — 'activationAccepted'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_79]** ⬜ non enrichi
  - Fragment : `key_exists('partyIntervention', $this->oceaneData) && is_array($this->oceaneData['partyIntervention'`
  - Question : Point de décision — key_exists('partyIntervention', 'this'->oceaneData) et is_array('this'->oceaneData['partyIntervention']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_80]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('relatedParty', $value) && is_array($value['relatedParty'])`
  - Question : Point de décision — is_array('value') et key_exists('relatedParty', 'value') et is_array('value'['relatedParty']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_81]** ⬜ non enrichi
  - Fragment : `is_array($party) && key_exists('@referredType', $party) && $party['@referredType'] == 'Organisation'`
  - Question : Point de décision — is_array('party') et key_exists('@referredType', 'party') et 'party'['@referredType'] égal à 'Organisation' et key_exists('role', 'party') et 'party'['role'] égal à 'TroubleResolutionContributor' et key_exists('id', 'party'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_82]** ⬜ non enrichi
  - Fragment : `$isContributor && key_exists('interventionStatus', $value) && is_array($value['interventionStatus'])`
  - Question : Point de décision — 'isContributor' et key_exists('interventionStatus', 'value') et is_array('value'['interventionStatus']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_83]** ⬜ non enrichi
  - Fragment : `$activationAccepted`
  - Question : Point de décision — 'activationAccepted'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_84]** ⬜ non enrichi
  - Fragment : `key_exists('criticity', $this->oceaneData) && is_array($this->oceaneData['criticity'])`
  - Question : Point de décision — key_exists('criticity', 'this'->oceaneData) et is_array('this'->oceaneData['criticity']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_85]** ⬜ non enrichi
  - Fragment : `$idAndLabel`
  - Question : Point de décision — 'idAndLabel'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_86]** ⬜ non enrichi
  - Fragment : `key_exists('targetRestorationDate', $this->oceaneData)`
  - Question : Point de décision — key_exists('targetRestorationDate', 'this'->oceaneData). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_87]** ⬜ non enrichi
  - Fragment : `!is_null($format)`
  - Question : Point de décision — !is_null('format'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_88]** ⬜ non enrichi
  - Fragment : `key_exists('plannedRestorationDate', $this->oceaneData)`
  - Question : Point de décision — key_exists('plannedRestorationDate', 'this'->oceaneData). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_89]** ⬜ non enrichi
  - Fragment : `!is_null($format)`
  - Question : Point de décision — !is_null('format'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_90]** ⬜ non enrichi
  - Fragment : `key_exists('partyIntervention', $this->oceaneData) && is_array($this->oceaneData['partyIntervention'`
  - Question : Point de décision — key_exists('partyIntervention', 'this'->oceaneData) et is_array('this'->oceaneData['partyIntervention']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_91]** ⬜ non enrichi
  - Fragment : `key_exists('relatedParty', $this->oceaneData['partyIntervention'][0]) && is_array($this->oceaneData[`
  - Question : Point de décision — key_exists('relatedParty', 'this'->oceaneData['partyIntervention'][0]) et is_array('this'->oceaneData['partyIntervention'][0]['relatedParty']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_92]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('actionInProgress', $value) && key_exists('startDate', $value['action`
  - Question : Point de décision — is_array('value') et key_exists('actionInProgress', 'value') et key_exists('startDate', 'value'['actionInProgress']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_93]** ⬜ non enrichi
  - Fragment : `!is_null($format)`
  - Question : Point de décision — !is_null('format'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_94]** ⬜ non enrichi
  - Fragment : `key_exists('partyIntervention', $this->oceaneData) && is_array($this->oceaneData['partyIntervention'`
  - Question : Point de décision — key_exists('partyIntervention', 'this'->oceaneData) et is_array('this'->oceaneData['partyIntervention']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_95]** ⬜ non enrichi
  - Fragment : `key_exists('relatedParty', $this->oceaneData['partyIntervention'][0]) && is_array($this->oceaneData[`
  - Question : Point de décision — key_exists('relatedParty', 'this'->oceaneData['partyIntervention'][0]) et is_array('this'->oceaneData['partyIntervention'][0]['relatedParty']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_96]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('actionInProgress', $value) && key_exists('description', $value['acti`
  - Question : Point de décision — is_array('value') et key_exists('actionInProgress', 'value') et key_exists('description', 'value'['actionInProgress']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_97]** ⬜ non enrichi
  - Fragment : `key_exists('partyIntervention', $this->oceaneData) && is_array($this->oceaneData['partyIntervention'`
  - Question : Point de décision — key_exists('partyIntervention', 'this'->oceaneData) et is_array('this'->oceaneData['partyIntervention']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_98]** ⬜ non enrichi
  - Fragment : `key_exists('relatedParty', $this->oceaneData['partyIntervention'][0]) && is_array($this->oceaneData[`
  - Question : Point de décision — key_exists('relatedParty', 'this'->oceaneData['partyIntervention'][0]) et is_array('this'->oceaneData['partyIntervention'][0]['relatedParty']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_99]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('id', $value)`
  - Question : Point de décision — is_array('value') et key_exists('id', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_100]** ⬜ non enrichi
  - Fragment : `$driDate != '' && $drcDate != ''`
  - Question : Point de décision — 'driDate' différent de '' et 'drcDate' différent de ''. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_101]** ⬜ non enrichi
  - Fragment : `$drcDate != ''`
  - Question : Point de décision — 'drcDate' différent de ''. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_102]** ⬜ non enrichi
  - Fragment : `$driDate != ''`
  - Question : Point de décision — 'driDate' différent de ''. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_103]** ⬜ non enrichi
  - Fragment : `key_exists('relatedResource', $this->oceaneData) && is_array($this->oceaneData['relatedResource'])`
  - Question : Point de décision — key_exists('relatedResource', 'this'->oceaneData) et is_array('this'->oceaneData['relatedResource']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_104]** ⬜ non enrichi
  - Fragment : `key_exists('resourceCharacteristic', $this->oceaneData['relatedResource'])`
  - Question : Point de décision — key_exists('resourceCharacteristic', 'this'->oceaneData['relatedResource']). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_105]** ⬜ non enrichi
  - Fragment : `is_array($value) && key_exists('id', $value) && $value['id'] == 'CLASITE'`
  - Question : Point de décision — is_array('value') et key_exists('id', 'value') et 'value'['id'] égal à 'CLASITE'. Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_106]** ⬜ non enrichi
  - Fragment : `key_exists('value', $value)`
  - Question : Point de décision — key_exists('value', 'value'). Quel est le comportement attendu dans le cas contraire ?
- [ ] **[block_107]** ⬜ non enrichi
  - Fragment : `key_exists('relatedResource', $this->oceaneData) && is_array($this->oceaneData['relatedResource']) &`
  - Question : Point de décision — key_exists('relatedResource', 'this'->oceaneData) et is_array('this'->oceaneData['relatedResource']) et key_exists('resourceSpecCharacteristic', 'this'->oceaneData['relatedResource']). Quel est le comportement attendu dans le cas contraire ?

## 📦 Services tiers non documentés
- [ ] **[Hbm\Common\Tools\StringTools]** ⬜ non enrichi
  - Fragment : `use: Hbm\Common\Tools\StringTools`
  - Question : Service tiers non documenté — Le composant 'Hbm\Common\Tools\StringTools' est utilisé dans ce périmètre sans équivalent identifié dans la cible. Quel est son rôle fonctionnel et quelles données échange-t-il avec le système ?
- [ ] **[Hbm\Globalapi\Service\Rest\ApiOceane]** ⬜ non enrichi
  - Fragment : `use: Hbm\Globalapi\Service\Rest\ApiOceane`
  - Question : Service tiers non documenté — Le composant 'Hbm\Globalapi\Service\Rest\ApiOceane' est utilisé dans ce périmètre sans équivalent identifié dans la cible. Quel est son rôle fonctionnel et quelles données échange-t-il avec le système ?
- [ ] **[Oft\Mvc\Application]** ⬜ non enrichi
  - Fragment : `use: Oft\Mvc\Application`
  - Question : Service tiers non documenté — Le composant 'Oft\Mvc\Application' est utilisé dans ce périmètre sans équivalent identifié dans la cible. Quel est son rôle fonctionnel et quelles données échange-t-il avec le système ?
- [ ] **[Zend\Json\Json]** ⬜ non enrichi
  - Fragment : `use: Zend\Json\Json`
  - Question : Service tiers non documenté — Le composant 'Zend\Json\Json' est utilisé dans ce périmètre sans équivalent identifié dans la cible. Quel est son rôle fonctionnel et quelles données échange-t-il avec le système ?
- [ ] **[App\Tools\OceaneTools]** ⬜ non enrichi
  - Fragment : `use: App\Tools\OceaneTools`
  - Question : Service tiers non documenté — Le composant 'App\Tools\OceaneTools' est utilisé dans ce périmètre sans équivalent identifié dans la cible. Quel est son rôle fonctionnel et quelles données échange-t-il avec le système ?
- [ ] **[ApiOceane]** ⬜ non enrichi
  - Fragment : `instantiation: ApiOceane`
  - Question : Service tiers non documenté — Le composant 'ApiOceane' est utilisé dans ce périmètre sans équivalent identifié dans la cible. Quel est son rôle fonctionnel et quelles données échange-t-il avec le système ?
